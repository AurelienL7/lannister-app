function Expense() {

    var expense;

    this.getExpense = function getExpense(){
        return expense;
    }

    this.setExpense = function setExpense(name, statut, date, label) {

        // Je dois vérifier si name existe bien parmis les utilisateurs inscrits

        // Je dois vérifier que le format date est bien valide

        // Je dois vérifier que le label est valide (ne permet pas d'insérer du code, etc)

    }

}
